# 教程重构方案（Phase 2）

配套：[教程审计报告](./tutorial_audit.md)。本文件回答"怎么改、改多少、不动什么"。

## 0. 三条硬约束

1. **不改章节顺序、不重编号**：现有 33 节的编号与 URL 全部保持（站点已上线，编号即 URL；且跨节引用、三篇导读、附录索引、README 目录都依赖编号）。
2. **不删内容**：现有 105,924 汉字全部保留。降级的内容只做**标注**（进阶 / 查阅），不做删除。
3. **体量预算**：新增 ≤ +13,000 汉字（约 +12%），明显低于审阅意见允许的 20–30%，把余量留给后续按需补充。

## 1. 新增一个锚点：1.4「从一句话到电机」

在第一章末尾新增一节（不动 1.1–1.3 的编号），承担四件事：

1. 给出**整机总图**：纵向功能链 4 层（任务与智能 / 行为与技能 / 运动与规划 / 实时控制）+ 驱动与嵌入式 + 机械执行；横向基础设施 6 类（ROS 与中间件、计算平台、安全、数据与日志、仿真与工具、配置模型与标定）。
2. **频率金字塔**：0.1–1 Hz（任务）→ 1–10 Hz（技能）→ 10–100 Hz（规划）→ 200 Hz–1 kHz（WBC/关节）→ 1–20 kHz（电流环）；一句话结论："越靠近物理世界，闭环越快；VLA 推理时低层不会停。"
3. **算法跑在哪里**：GPU（VLA/视觉）→ CPU（任务/规划）→ 实时 CPU（估计/WBC）→ MCU（关节/电流环）→ 总线 → 电机。
4. **两个闭环**：在线运行闭环 vs 离线开发闭环（明确"运行时训练吗""reward 还在吗""Isaac Lab 部署时跑吗""遥操作数据何时用"）。

同时把这张图变成**全书的回归锚点**：每一节开头加一句"在整机里的位置"（一句话，不新增段落）。

## 2. 各节改动清单

| 节 | 改动类型 | 具体做什么 | 预计新增 |
| --- | --- | --- | --- |
| 1.3 系统架构 | **重画图 + 改措辞** | 把"五层并列"改成"4 层纵向功能链 + 横向基础设施"；补 ROS 最小运行链（进程/节点/Topic → SDK → 总线 → MCU）与计算拓扑 | +1,500 |
| **1.4（新）** | **新增** | 见上（总图 + 频率金字塔 + 计算拓扑 + 两个闭环 + 案例走一遍） | +2,500 |
| 2.x | 只加标注 | 每节加"在整机里的位置"一句；2.1 的 PR/AB、关节索引标注为"查阅" | +200 |
| 3.3 传感器 | **扩写** | 新增"力与触觉：机器人怎么知道碰到了"（力/力矩、触觉、接触检测、滑移、接触状态；不深入硬件原理） | +1,200 |
| 3.6 电气安全 | **扩写** | 新增"运行时安全过滤"：VLA/Agent 不是最高执行权限；限位/碰撞/速度/力矩/工作空间/命令新鲜度/运动学可行性 | +1,000 |
| 4.1 数学基础 | **只改叙事** | H2 顺序与内容不动；改写导语与过渡，让它从"物体在哪里 → 需要坐标 → 需要姿态 → 需要表示"引出，而不是从数学目录引出 | +300 |
| 4.5 / 4.6 | 只加标注 + 一句 | 标注 Level 2 细节（QP、动力学项）；明确"学习方法也可以跑在这一层" | +200 |
| 5.1 AI 词汇 | **扩写为"智能分层"** | 新增两个 H2：「智能分层：任务层／技能层／运动层／控制层」与「层级与方法类型是两个维度」（Model-based / Optimization / Learning / Foundation 可落在任意层）；保留原有词汇表 | +2,200 |
| 5.3 运动规划 | **扩写** | 新增"导航：从客厅自己走到厨房"（SLAM→地图→全局规划→局部避障→落脚点→运动控制器） | +1,200 |
| 5.4 操作 | **扩写** | 新增"移动与操作是一个整体（Loco-Manipulation）"：搬箱子、开门、推物；腰/腿/臂共同参与 | +800 |
| 5.5 学习控制 | **重组 + 扩写** | 把已有的遥操作/数据集/IL/RL/Sim2Real 素材重组为"数据飞轮"：部署→记录→失败挖掘→清洗标注→数据集→训练→评测→部署；并加"评测与泛化：Demo ≠ 能力" | +1,800 |
| 5.6 VLA | **扩写 + 改开头** | 新增"当前观测不够时：State 与 Memory"；把开头从"TF 式专业问题"换成生活化问题再引术语（原案例保留在后文） | +900 |
| 5.7 WAM | 只加标注 | 标注为"研究前沿"；明确它不是一台机器人的必需件 | +100 |
| 6.1 ROS | **改开头** | 先给"相机程序算出了目标，控制程序怎么拿到？"再引 Node/Topic/TF；原 TF 报错案例保留 | +150 |
| 6.x 工具链 | 只加标注 | 各节标注"何时需要"（离线开发 / 部署 / 调试） | +200 |
| 三篇导读 | **扩写** | 说明三级阅读：主干（Level 1）/ 进阶（Level 2）/ 查阅（Level 3）；说明"基础必备 / 现代常用 / 研究前沿"标签含义 | +600 |
| 附录 | **新增一页** | 单页版《整机总图》（纵向链 + 横向基础设施 + 频率 + 计算平台 + 两个闭环），供回看 | +400 |

合计新增约 **+15,250 汉字**（含图注），若按"正文汉字"口径约 +13,000–14,000，仍在预算内（+12–14%）。

## 2.5 Phase 3 第一批：零设计成本的硬伤修复

这些不需要新架构决策，先做完再动大结构（每条都已在审计报告 §4.5 核实）：

| # | 修什么 | 位置 | 动作 |
| --- | --- | --- | --- |
| 1 | 学生导读让读者找“章级小结表格”，但全书没有 | 00-student | 改一句话 + 补附录入口 |
| 2 | 产品导读读法在 3 节失效（定义晚于首条结论） | 00-product-pr | 精确化读法：读到“首个术语定义 + 紧随其后的第一条结论” |
| 3 | 附录没有入口、编号 F 在最前 | 07-appendix + 各章 | 重排编号 + 正文加入口链接 |
| 4 | 3.4 相邻两段逐字重复 | 3.4:126/128 | 合并 |
| 5 | 3.4/3.5 同一比喻用两遍 | 3.5 | 改回指 |
| 6 | 5.6 唯一完整链路埋在评测小节 | 5.6:125 | 挪出独立成小节 + 回指 1.3 |
| 7 | “回采/再训练”全书 0 次 | 5.5 | 补 150 字闭环 |
| 8 | 5.7 悬空引用“4.6 节的 MPC” | 5.7:73 | 改指 5.3，或改写为“4.6 的 LIPM” |
| 9 | 5.1 前向引用错误（写“见前面”实际在后面）+ 分工表漏“任务/行为层”一行 | 5.1 | 修正 + 补行 |
| 10 | 5.6:63 误引 5.2（“周身广角”，5.2 并未给出该配置） | 5.6 | 改引 5.2 的“头部深度 + 多视角”表述 |
| 11 | 1.2/1.3/4.4 术语级 H3 缺结论句 | 1.2、1.3、4.4 | 各补一条短句 |
| 12 | 第 1–2 章“见 X 节”不可点击；1.3/2.5 无章末过渡 | ch1–2 | 改链接 + 补过渡句 |
| 13 | 3.1 电源树位置与导读口径不符 | 3.1 | 节内前移 |
| 14 | 2.1 含消息字段（与 3.2/附录 B/C 重复） | 2.1 | 移到 3.2/附录 C，主干瘦身 |

## 3. 明确**不做**的事

- 不新增"最新模型介绍"（π 系列、GR00T、Helix…）：只在需要举例时点名 1–2 个，讲"这一类技术在系统里的角色"。
- 不重写语言：全书语言刚做过专项审计；本轮只改审计卡片标出的"认知跳跃"句。
- 不动章节顺序、不改节号、不改 URL；不删除任何现有内容。
- 不为每节都强行套"输入/输出/上游/下游/频率"模板：只对约 12 个核心模块补齐频率，其余保持现有风格（避免形式主义膨胀）。

## 4. Phase 3 试点（先改三处，验证模板再铺开）

1. **1.3 + 1.4**：验证"重画骨架 + 新增锚点"的写法；
2. **5.1**：验证"在既有节内扩写为智能分层"的写法（不新增节号）；
3. **5.5**：验证"用现有素材重组为闭环叙事"的写法（零新增知识）。

三处改完先做构建 + 链接 + 术语一致性检查，再决定是否铺开到其余各节。

## 5. Phase 5 一致性检查清单（铺开后逐条过）

- 同一术语是否在多节给出不同说法（重点：状态估计 vs 感知、规划 vs 控制、WBC vs 关节控制、VLA vs 控制器）。
- "五层"与"纵向链 + 横向基础设施"两套说法是否共存造成混乱（旧说法要全部改口径）。
- 频率数字是否全书一致（以 1.4 的表为准，逐节校对）。
- Brain / Cerebellum 比喻是否与"层级≠方法类型"冲突。
- 每节是否都有"在整机里的位置"且指向同一张图。
- 三篇导读的节号、时长、入口描述是否与新结构一致。

## 6. 验收对应（审阅意见 §28）

| 验收项 | 由谁满足 |
| --- | --- |
| A 系统认知（完整链路 + 反馈链） | 1.4（总图）+ 5.1（分层）+ 5.3（导航链条） |
| B 模块边界（8 组区分） | 1.4 的层级表 + 5.1 的两维度 + 各节"位置"句 |
| C 时间尺度 | 1.4 频率金字塔 + 全书频率口径统一 |
| D 软件与硬件 | 1.4 计算拓扑 + 6.1 运行链 |
| E AI 与系统关系 | 5.1 两维度 + 3.6 运行时安全过滤 |
| F 自主性（失败与恢复） | 5.1 的失败恢复 H2 + 3.6 |
| G 数据闭环 | 5.5 数据飞轮 + 评测 |
| H 阅读体验（不必记住工程细节） | 三级标注 + 附录单页图 |

---

# 附录 A：整机总图（草案 v1，Phase 3 落进正文）

用途：作为全书唯一的总图，1.3 定义、每节回归。目标读者读完能自己画出它。

## 一、纵向功能链（信息自上而下，闭环自慢而快）

| # | 层 | 它回答的问题 | 输入 | 输出 | 典型频率 | 谁负责（书中位置） |
| --- | --- | --- | --- | --- | --- | --- |
| 5 | 任务与智能层 | 用户到底要什么？先做哪一步？ | 语言指令、当前世界状态、记忆 | 子任务序列 / 技能调用 | 0.1–1 Hz | 新增：任务与技能层（§9） |
| 4 | 行为与技能层 | 这一步怎么做才不失败？做完了吗？ | 子任务、世界状态 | 子目标（位姿、抓取点、落脚点） | 1–10 Hz | 新增：同上（技能、前置/后置条件、失败恢复） |
| 3 | 运动与规划层 | 从当前位置怎么过去/怎么够到？ | 子目标、地图、机器人状态 | 轨迹 / 落脚点序列 | 10–100 Hz | 5.3 运动规划（+§11 导航链条） |
| 2 | 实时控制层 | 这一毫秒全身各关节出多大力？ | 轨迹、姿态、接触力 | 关节目标 / 力矩 | 200 Hz–1 kHz | 4.5 基础控制、4.6 WBC |
| 1 | 驱动与嵌入式层 | 怎么把力矩指令变成绕组电流？ | 关节指令 | 电流、力矩、状态回传 | 1–20 kHz（电流环） | 3.2 伺服、3.5 MCU、3.4 总线 |
| 0 | 机械执行层 | 力如何变成身体运动与接触？ | 力矩 | 关节运动、接触力 | 物理世界（连续） | 2.1–2.4 机构/结构/传动/接触 |

> 一句话：**越靠近物理世界，闭环越快**。VLA 一次推理要几百毫秒，而在这几百毫秒里，实时控制层已经跑了几十到几百个周期——它不会停下来等上层。

## 二、传感与状态（反馈链，自下而上）

| 传感器 | 提供什么 | 去哪 | 频率 |
| --- | --- | --- | --- |
| 编码器 | 关节角、角速度 | 驱动器内部的电流/速度/位置环 | 与电流环同周期（1–20 kHz） |
| IMU | 姿态、角速度、加速度 | 状态估计 → 平衡控制 | 200 Hz–1 kHz |
| 相机（RGB/深度） | 图像、点云 | 感知（检测/分割/位姿）→ 世界状态 | 10–30 Hz |
| LiDAR | 点云、地图 | SLAM / 导航 | 10–20 Hz |
| 力/力矩、足底、触觉 | 接触力、滑移、接触状态 | 阻抗/力控、抓取判定、失败检测 | 100 Hz–1 kHz |

感知与状态估计的输出**同时喂给三层**：任务层（世界变了吗）、规划层（障碍/目标）、控制层（自身状态）。

## 三、横向基础设施（每一层都要用，但不属于任何一层）

| 基础设施 | 它做什么 | 书中位置 |
| --- | --- | --- |
| 中间件与软件架构 | 把算法变成进程/节点，用 Topic/Service 连起来（ROS 2 / DDS） | 6.1 |
| 计算平台 | GPU（VLA/视觉）→ CPU（任务/规划）→ 实时 CPU（估计/WBC）→ MCU（关节/电流环） | 3.5 + 新增"算法跑在哪里" |
| 安全 | 硬件保护（急停/过流/过温/超时）+ 运行时安全过滤（限位/碰撞/可行性/命令新鲜度） | 3.6 + §17 |
| 数据与日志 | 记录 → 回放 → 失败挖掘 → 数据集 | 5.5 + §14 |
| 仿真与工具 | MuJoCo / Isaac / Gazebo / RViz / Foxglove | 6.2–6.5 |
| 配置、模型与标定 | URDF/MJCF、参数、固件、版本 | 2.5、6.6 |

> 关键澄清：**ROS 不在"大脑下面"**。它是横向的软件基础设施，大脑、小脑、感知、仿真都在用它。

## 四、两个闭环（不要混）

**在线运行闭环**（机器人身上，每个频率各有一圈）
传感 → 感知/状态估计 → 任务/规划/策略 → 安全过滤 → 控制 → 驱动 → 电机 → 环境 → 传感 → …

**离线开发闭环**（开发机上，不跑在机器人上）
遥操作/仿真/真机日志 → 数据集 → 训练 → 评测 → 导出部署 → 回到机器人 → 继续采集

> 运行时**不训练**：真机上没有 reward、没有优化器；Isaac Lab 只在离线侧出现。遥操作数据只在离线侧被消费。

## 五、用统一案例走一遍

案例：**“走到桌子旁，把红色杯子拿起来。”**

| 阶段 | 谁负责 | 输入 | 输出 | 频率 |
| --- | --- | --- | --- | --- |
| 听懂 | 任务层（LLM/VLA 的语言部分） | 语音/文字 | 结构化目标：找红色杯子、拿起来 | 0.1–1 Hz |
| 组织 | 任务层 + 技能层 | 目标、世界状态 | 子任务：导航到桌前 → 抓取 → 抬起 | 0.1–10 Hz |
| 过去 | 导航（SLAM→全局→局部→落脚） | 地图、当前位姿 | 落脚点序列 / 速度指令 | 10–100 Hz |
| 够到 | 操作规划 | 杯子位姿、机器人状态 | 末端轨迹 / 抓取位姿 | 10–100 Hz |
| 拿住 | 实时控制（WBC + 力控） | 轨迹、接触力 | 关节目标/力矩 | 200 Hz–1 kHz |
| 出力 | 驱动与嵌入式 | 关节指令 | 电流、力矩 | 1–20 kHz |
| 失败 | 技能层（判定）+ 任务层（决策） | 观察结果 | 重试 / 换视角 / 换抓取 / 换站姿 / 求助 / 安全停 | 1–10 Hz |

## 六、这张图要用在哪里

1. **1.3**：完整给出（纵向链 + 横向基础设施 + 频率金字塔 + 计算拓扑 + 两个闭环）。
2. **每节开头**：一句"在整机里的位置"（放它在这张图的哪一格）。
3. **新增的任务/技能层一节**：把最上面两格补上（当前最大的缺口）。
4. **附录**：给一张可打印的单页版，方便回看。

## 7. 执行记录

| 轮次 | 内容 | 提交 |
| --- | --- | --- |
| 1 | Phase 1 审计（40 张逐节卡片 + 跨节问题 X1–X21）+ Phase 2 方案 | \`f3b2c26\` |
| 2 | Phase 3 第一批（上）：导读 2 处硬伤、附录编号 A–D、3.4 重复段合并、5.6 链路独立成节、5.5 数据闭环、5.7 MPC 引用、5.1 前向引用与分工表、5.6 误引 | \`2e33b8c\` |
| 3 | **试点 1**：新增 **1.4《从一句话到电机：整机总图》**（2920 汉字、5 条结论：总图 / 频率金字塔 / 计算平台 / 两个闭环 / 案例走一遍）；1.3 重构为“四层功能链 + 横向基础设施”并指向 1.4；级联更新三篇导读、README、首页卡片 | 本轮 |

执行后的口径变化（都已复算）：

- 正文 **33 → 34 节**；正文汉字 105,924 → **109,338**（+3.2%，远低于 +20–30% 预算）；
- “一句话听懂” 285 → **291** 条；产品导读 21 → **22 节**、70 → **75 分钟**；学生导读第 1 章 40 → **55 分钟**（全书 550 分钟，仍在 8–10 小时区间）；
- 站点 42 → **43 页**；站内链接 2372 条 0 失效；产品导引用例校验 22/22 通过。

尚未执行（下一轮起）：Phase 3 第一批剩余 4 项（3.4/3.5 比喻重复、1.2/1.3/4.4 缺结论句、第 1–2 章链接化、3.1 电源树前移、2.1 PR/AB 移出）、试点 2（5.1 智能分层）、试点 3（5.5 数据闭环已部分完成）、以及 5.3/5.4/3.3/3.6 的扩写。
| 4 | **试点 2**：5.1 加“层级与方法类型是两个维度”（4×4 表 + 两个反例：RL 策略在低层、传统规划在高层）与分工表“层级”列；**1.3 补齐 11 个 H3 的结论句**；**全书 219 处“见 X 节”变成可点击链接** | 本轮 |
| 5 | **试点 3**：5.5 加「评测与泛化：演示成功不等于能力达标」（Demo/Benchmark/Deployment 三证据 + 六个测试维度 + 两条工程习惯）；1.2/4.4 补齐缺失结论句；3.5 与 3.4 重复定义改回指 | 本轮 |
| 5（续） | **给全部 33 节加“在整机里的位置”**（块引用：层名 + 上游 + 下游 + 指向 1.4），由 4 个编辑子代理分区完成；统一了层名（对齐总图）与标点体例 | 本轮 |
