# 5.7 WAM：世界动作模型

## 先看一个现场问题

VLA 模型看懂了“把红杯子拿起来”，也输出了一段抓取动作；但执行到一半，杯子被人向右挪了 10 厘米。模型继续按原来的视觉记忆推手臂，最后抓了一把空气。换一种做法：模型在真正执行前先预测“如果手臂继续向左，下一帧会看到什么、末端会碰到什么”，发现预测画面和当前观测不一致，于是提前停止并重新规划。

现场通常会这样问：

> “VLA 已经在输出动作了，为什么还要一个世界模型？WAM 是更会说话的 VLA，还是一套能预测未来的仿真器？”

WAM（World Action Model，世界动作模型）是一类同时建模“世界如何变化”和“机器人应该采取什么动作”的学习模型。[1][2] 与只从当前观测直接回归动作的 VLA 相比，WAM 的核心增量是把动作和未来状态放进同一个预测问题：给定当前观测、机器人状态和候选动作，模型预测未来观测、未来状态或动作序列。

## WAM 与 VLA 的分工

两者都属于第 5 章的高级策略层，但回答的问题不同：

| 模型 | 核心问题 | 典型输入 | 典型输出 | 更擅长什么 |
| --- | --- | --- | --- | --- |
| VLA | 根据任务和场景，下一步做什么 | 图像、语言、机器人状态 | 动作 token、动作向量、动作块 | 任务理解、语义绑定、操作策略 |
| WAM | 采取某个动作后，世界会怎样变化 | 图像/深度、状态、候选动作、历史 | 未来观测、未来状态、动作序列 | 预测后果、滚动决策、策略评估 |

VLA 不一定显式预测未来观测；WAM 则把“动作—世界变化”的关系作为学习目标。[1][2] 因此，WAM 可以在执行前比较多个候选动作，也可以作为策略直接输出动作。它既像策略模型，又像可学习的世界模型（World Model），边界取决于具体实现。

## WAM 的三种工程用法

### 1. 作为策略

模型直接输出动作序列，和 VLA 一样接入动作块执行链路。区别在于训练目标中通常包含未来观测或未来状态预测，模型不仅学习“做什么”，还学习“这样做的后果”。

### 2. 作为可学习仿真器

在执行前，用 WAM 对多个候选动作做短期滚动预测，选择代价更低或更接近目标的动作。这种用法接近模型预测控制的思想，但预测模型不再完全由人工写成刚体动力学方程，而是从数据中学习。

### 3. 作为教师模型

大型 WAM 可以离线预测未来表征，再把这种“知道未来会发生什么”的能力蒸馏给更小的在线策略。[4] 部署时机器人只运行小模型，不需要每次都让视频级大模型显式生成未来画面。这个方向适合算力受限的机器人本体。

## 人形机器人为什么更需要 WAM

固定底座机械臂的世界里，相机通常不动，动作主要来自手臂。人形机器人或移动操作机器人则同时存在：

- 相机随头部和躯干运动；
- 浮动基座改变观测视角；
- 手臂运动会反过来影响身体姿态；
- 足底接触变化会改变后续可行动作。

DECOWAM 这类工作专门把相机自我运动、底盘运动和手臂动作拆开建模，说明 WAM 在腿式移动操作中的关键不是“生成更漂亮的视频”，而是分清不同身体部分如何共同影响未来观测。[3] 对 G1 这类平台，WAM 的输入不应只有相机图像，还应包含关节状态、浮动基座估计和接触信息；输出也不能只停留在手臂末端，还要考虑腰部、支撑腿和全身稳定性。

## WAM 的边界

WAM 的预测仍然是学习出来的近似，不是物理定律本身。它会遇到分布外场景、长期滚动误差、接触细节错误和训练数据偏差。预测画面看起来合理，不代表对应力矩、摩擦和接触力一定真实。

因此 WAM 仍然属于第 5 章的“大脑”层：它可以预测、评估和生成动作意图，但不能绕过第 4 章的状态估计、全身控制、限幅和保护链路。是否把预测结果交给底层执行，必须经过 5.8 节的接口契约和安全过滤。

## 参考资料

[1] Lopetegui Gonzalez, J. A., Pacaud, P., & Schmid, C. “Spatially Aware World Action Model via Geometric Latent Diffusion.” *arXiv*, 2026. WAM 联合动作、RGB 与深度预测的代表工作。<https://arxiv.org/abs/2609.02531>

[2] Sun, H., et al. “Riemann-1.0: An Embodied World Action Model for Physical AI.” *arXiv*, 2026. 将世界演化与机器人动作统一为因果序列建模。<https://arxiv.org/abs/2608.27033>

[3] Ma, S., et al. “DECOWAM: Decoupled Whole-Body World-Action Model for Legged Mobile Manipulation.” *arXiv*, 2026. 腿式移动操作中的相机自我运动、底盘与手臂动作解耦。<https://arxiv.org/abs/2608.20114>

[4] Ma, S., et al. “ForeTime-VLA: Causal Future-Token Distillation from a World Action Model for Conveyor-Belt Manipulation.” *arXiv*, 2026. WAM 作为教师模型向在线 VLA 策略蒸馏未来表征。<https://arxiv.org/abs/2608.20735>
